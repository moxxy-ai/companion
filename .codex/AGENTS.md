# AGENTS.md — Companion (Codex scope)

This is a pointer, not a copy. The canonical, universal instructions live in the
repository-root [`/AGENTS.md`](../AGENTS.md), which Codex reads automatically
when a session runs at the repo root. The deep knowledge base is in
[`../.ai/`](../.ai/README.md) (skills + agent roles), also symlinked here as
`skills/` and `agents/`.

**Read `/AGENTS.md` first**, then load the relevant `../.ai/skills/*/SKILL.md`
for the task. Nothing Companion-specific is duplicated in this directory, so
there is nothing here to keep in sync.
